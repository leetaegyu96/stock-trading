# v1.13.1 — 2026-07-21

**패치: 시드/리플레이 보유 red_score 승계 (#7 핵심).**

## Fixed

- 리플레이/시드가 마지막 거래일 유니버스에서 빠진 보유 종목의 `red_score`를 0으로 리셋해, 시드
  대시보드에서 해당 보유가 "무위험"으로 오인될 수 있던 문제 수정. 이제 종목별 직전값(last-seen)을
  승계한다(라이브 on_close와 동일한 의도). (#7)

## Refactored

- 보유 `signal_status` 행 생성을 `simcore/signal_status.py::holding_signal_row` 공용 헬퍼로 통합 —
  리플레이·라이브 오케스트레이터가 동일 함수를 써 stop_px/trail_px 공식 드리프트를 원천 차단.

## 검증

- pytest 366 passed(승계 회귀 테스트로 버그 재현→수정 실증), 동치성·시드 테스트 통과, 회귀 0.

## 후속

- #7 나머지(dashboard positions 중복 조회, lifecycles docstring 등) 계속 백로그.
- #32 팩터 앙상블·리스크-인지 스코어링은 전용 설계 사이클로 보류(핵심 스코어링 재설계).
